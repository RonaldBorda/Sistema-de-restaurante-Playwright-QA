package tests;import base.BaseTestPlaywright;import com.microsoft.playwright.Page;import org.junit.jupiter.api.Test;import java.nio.file.Paths;import static org.junit.jupiter.api.Assertions.assertTrue;public class HP17LoginCamposVaciosPlaywrightTest extends BaseTestPlaywright {    @Test    void loginConCamposVacios() {        page.navigate("https://ernest.xo.je/index.php?route=auth/login");        // No se llenan los campos        page.locator("button[type='submit']")                .click();        page.waitForLoadState();        assertTrue(                page.locator("input[name='username']").isVisible(),                "El campo Usuario dejó de mostrarse."        );        assertTrue(                page.locator("input[name='password']").isVisible(),                "El campo Contraseña dejó de mostrarse."        );        assertTrue(                page.url().contains("login"),                "El sistema permitió continuar con los campos vacíos."        );        page.screenshot(                new Page.ScreenshotOptions()                        .setPath(Paths.get(                                "evidencias/HP17-login-campos-vacios.png"))                        .setFullPage(true)        );    }}