package tests;import base.BaseTestPlaywright;import com.microsoft.playwright.Page;import org.junit.jupiter.api.Test;import java.nio.file.Paths;import static org.junit.jupiter.api.Assertions.assertTrue;public class HP05FlujoCompletoEstadosPlaywrightTest extends BaseTestPlaywright {    @Test    void flujoCompletoEstadosPedido() {        page.navigate("https://ernest.xo.je/index.php?route=auth/login");        page.locator("input[name='username']").fill("cocinero");        page.locator("input[name='password']").fill("123");        page.locator("button[type='submit']").click();        page.waitForTimeout(2000);        long pendientesAntes =                page.locator("button:has-text('Comenzar Preparación')")                        .count();        long preparacionAntes =                page.locator("button:has-text('Marcar como entregado')")                        .count();        assertTrue(                pendientesAntes > 0,                "No existen pedidos pendientes."        );        // Pendiente -> En Preparación        page.locator("button:has-text('Comenzar Preparación')")                .first()                .click();        page.waitForTimeout(2000);        long pendientesDespues =                page.locator("button:has-text('Comenzar Preparación')")                        .count();        long preparacionDespues =                page.locator("button:has-text('Marcar como entregado')")                        .count();        assertTrue(                pendientesDespues < pendientesAntes,                "El pedido no salió de Pendientes."        );        assertTrue(                preparacionDespues > preparacionAntes,                "El pedido no pasó a En Preparación."        );        // En Preparación -> Entregado        page.locator("button:has-text('Marcar como entregado')")                .first()                .click();        page.waitForTimeout(2000);        long preparacionFinal =                page.locator("button:has-text('Marcar como entregado')")                        .count();        assertTrue(                preparacionFinal < preparacionDespues,                "El pedido no fue entregado."        );        page.screenshot(                new Page.ScreenshotOptions()                        .setPath(Paths.get(                                "evidencias/HP05-flujo-completo.png"))                        .setFullPage(true)        );    }}